---
status: accepted
---

# 托管执行会话限定在一个可审计重任务内

每个托管执行会话只服务一个边界明确的可审计重任务，OfferU 持久化其任务输入、执行器、标准化事件、待确认动作、结果和恢复位置；会话可以中断和恢复，完成后封存，不跨任务继承外部 Coding Agent 的隐藏对话上下文。跨任务连续性来自 OfferU 已确认的领域事实、学习观察和显式任务材料，而不是长期 Codex 或 Claude Code 会话。选择任务级会话是为了保留多轮执行与恢复能力，同时避免外部上下文成为不可检查的第二记忆源。
