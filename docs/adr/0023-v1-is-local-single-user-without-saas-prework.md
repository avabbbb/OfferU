---
status: accepted
supersedes: 0001
---

# 第一阶段只建设本地单人版且不预埋 SaaS 模型

OfferU 第一阶段只有一个本地拥有者和一份本地数据空间。当前不引入 `workspace_id`、租户、组织、登录、计费或多租户权限，也不以未来可能性增加领域模型复杂度；系统只需把唯一 LocalOwner/Profile 作为显式本地仓储约束，避免各模块各自创建默认记录。若未来产生明确 SaaS 需求，将依据届时的同步、身份和隔离目标重新进行独立架构设计。
