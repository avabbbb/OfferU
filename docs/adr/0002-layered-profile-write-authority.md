---
status: accepted
---

# Profile 采用分层写权限

OfferU 将使用者模型拆分为职业证据、求职偏好和职业假设：职业证据只有在使用者确认后才能写入；求职偏好允许主 Agent 自动更新，但每次变化必须可见、可审计、可撤销；职业假设独立保存为待验证内容，不能被简历、岗位匹配或面试模块当作事实引用。该边界优先保护事实可信度，同时避免所有低风险偏好变化都打断使用者。
